# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'gash' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'BigInt', '~> 3.1'

  # Pods for gash

  target 'gashTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'gashUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
